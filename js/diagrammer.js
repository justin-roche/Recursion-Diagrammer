(function(){

	app = {}; 

/*App.view accepts input text describing a named function that makes a recursive call.
App.Preprocessor  modifies the text so that the recursive calls invoke a spy function (Spy.js).
The spy function makes the call after storing args and return
values in a linked list, which is displayed by ListConverter using the jsOrg library.*/
	
}

	

)()